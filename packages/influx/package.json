{
  "name": "@acme/influx",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -e ../../.env --",
    "update": "pnpm update"
  },
  "dependencies": {
    "@influxdata/influxdb-client": "^1.33.2"
  },
  "devDependencies": {
    "dotenv-cli": "^6.0.0",
    "typescript": "^4.9.5"
  }
}